# Accessibility

- Keyboardだけで操作可能
- 色に加えてshape、label、status textを使う
- flashing animationを使わない
- motionはsingle-screen内のlinear movementだけ
- 音がなくても全eventを理解できる

Remapping、gamepad、subtitle customizationは後続phaseへdeferする。
